Work inside the EXISTING POS project in this workspace. Do NOT create a new project.

GROUND RULES (apply to every step):
- Before editing any file, READ its current contents first.
- Before referencing any database table or column, inspect the actual schema in src/db/migrations/*.sql. NEVER assume table or column names — verify them.
- Do NOT change, refactor, or "improve" any unrelated code, logic, layout, or behavior.
- RTL layout: use logical CSS only (ms-/me-/ps-/pe-/start-/end-). No left/right.
- Brand identity: copper #CF694A, beige background #F9F8F5. Keep it.
- Money values are integers (1 dinar = 100 fils). Currency label is "د.أ".
- Reuse existing utilities (formatMoney, existing query/report functions). Do not duplicate logic.
- If a required file, function, or symbol cannot be found, or anything is ambiguous, STOP and report it — do NOT guess or invent.
- After each section: run `npx tsc --noEmit`, fix all errors, then create a git commit.

TASK: Add financial summary widgets to the Dashboard page.

SECTION T1 — "Available Liquidity" card
- On the Dashboard page, add a prominent card titled "السيولة المتاحة".
- Total = sum of `balance` of all active accounts whose `type` is one of cash/card/bank/wallet. EXCLUDE receivable/payable.
- First inspect the `accounts` table schema and the existing accounts query file to confirm exact column names before writing the query.
- Below the total, show a small breakdown: cash, banks, wallets, cards.
- Use a stable React Query key; invalidate it after any sale or expense is recorded.

SECTION T2 — Profit cards: daily / weekly / monthly
- Add three cards: profit today, profit this week, profit this month.
- Net profit for a period = sales revenue − cost of goods sold − expenses, for invoices with status='active' only.
- REUSE the existing period-report logic (locate the reports query file). Do NOT write parallel duplicate queries. If no such function exists, report this before proceeding.
- Each card shows the value formatted with formatMoney and the period label.

ACCEPTANCE CRITERIA:
- `npx tsc --noEmit` → 0 errors.
- Liquidity card shows correct total and breakdown.
- The three profit cards show correct values and refresh after a sale/expense.
- No unrelated code or behavior changed.

After each section commit. After T2: `git push origin main`.