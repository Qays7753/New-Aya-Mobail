اعمل داخل مشروع POS. إصلاحات أمان وسلامة بيانات. نفّذ الأقسام
بالترتيب، كل قسم = commit. ثوابت: RTL logical CSS، النحاسي
#CF694A، المبالغ integer (1 دينار = 100 قرش).
P1 — منع المخزون السالب
ملف: src/db/queries/sales.ts (completeSale)
- قبل بناء دفعة الـ SQL: اجلب stock_qty الحالي لكل منتج في
  السلة مفعّل track_stock
- إن كانت الكمية المطلوبة لأي منتج تتجاوز المخزون المتاح،
  ارفض البيع كاملاً وألقِ خطأ عربياً واضحاً يذكر اسم المنتج
  والمتاح، مثل: "الكمية غير متوفرة للمنتج X (المتاح: N)"
- لا تسمح أبداً بأن يصبح stock_qty سالباً
P2 — التحقق من مبلغ الاسترجاع
ملف: src/db/queries/sales.ts (returnInvoice)
- قبل تنفيذ الاسترجاع: احسب مجموع refunds.amount
- إن كان المجموع ≤ 0 أو > invoice.paid_amount، ألقِ خطأ
  عربياً واضحاً: "مبلغ الاسترجاع يتجاوز المبلغ المدفوع"
- ارفض أي صف استرجاع بقيمة سالبة
P3 — إزالة مسار استعادة كلمة السر غير الآمن
- src/lib/auth.ts: احذف بالكامل تعريف window.resetPins
  والتعليق فوقه
- src/components/auth/DailyLockScreen.tsx: احذف زر "نسيت كلمة
  المرور؟" واستدعاءه
- src/components/auth/AdminPinDialog.tsx: احذف نفس الزر
  واستدعاءه
- لا تُضِف مسار استعادة بديلاً؛ استعادة كلمة السر تتم لاحقاً
  عبر استعادة نسخة احتياطية فقط
P4 — سجل تدقيق للعمليات الحساسة
- أنشئ ملف هجرة جديداً بالرقم التالي المتاح، فيه جدول:
  audit_log(id TEXT PK, ts TEXT NOT NULL, action TEXT NOT NULL,
  detail TEXT, ref_type TEXT, ref_id TEXT)
  وسجّله في migrations/index.ts حسب النمط الموجود
- أضف دالة مساعدة logAudit(action, detail, refType?, refId?)
  في src/db/queries (ملف audit.ts) تُدرج صفاً في audit_log
- استدعِ logAudit عند: إتمام بيع، استرجاع فاتورة، تغيير
  PIN المشرف، تغيير القفل اليومي، استعادة نسخة احتياطية،
  تعديل سعر منتج
- أضف عارضاً للسجل في صفحة الإعدادات (تبويب أو قسم): قائمة
  للقراءة فقط، الأحدث أولاً، التاريخ + نوع العملية + الوصف
معايير القبول:
- [ ] npx tsc --noEmit → 0 أخطاء
- [ ] بيع كمية تفوق المخزون يُرفض برسالة واضحة
- [ ] استرجاع يتجاوز المدفوع يُرفض
- [ ] لا أثر لـ resetPins ولا زر "نسيت كلمة المرور؟"
- [ ] العمليات الحساسة تُسجَّل وتظهر في الإعدادات
بعد كل قسم commit، وبعد P4: git push origin main