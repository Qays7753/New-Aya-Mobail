Work inside the EXISTING POS project. Do NOT create a new project.

GROUND RULES (apply to every step):
- Before editing any file, READ its current contents first.
- Before referencing any DB table/column, inspect the actual schema. NEVER assume — verify.
- Do NOT change/refactor unrelated code, logic, or behavior. These are targeted fixes only.
- RTL: logical CSS only (ms-/me-/ps-/pe-/start-/end-).
- Brand: copper #CF694A, beige #F9F8F5.
- Currency label "د.أ".
- If a required file/symbol is missing or anything is ambiguous, STOP and report — do NOT guess.
- After each section: run `npx tsc --noEmit`, fix errors, commit.

TASK: Streamline checkout, fix the tablet/phone cart layout, default the expense account, and fix dialog closing.

SECTION Q1 — Faster sale completion
- READ the current payment/checkout dialog first.
- Provide a default ONE-TAP path that completes the sale as cash, exact amount, using the main cash account directly.
- Advanced payment options (other account, change calculation, split payment) must be available ON DEMAND only — not forced on every sale.
- Rename every user-facing occurrence of the word "الدفع" to "تسجيل البيع" (UI text only — do NOT rename code symbols or functions).

SECTION Q2 — Persistent cart side panel
- READ how the cart sidebar visibility is currently controlled.
- The cart must show as a fixed side panel on ALL tablet sizes.
- Lower the breakpoint so the cart only collapses into a bottom-sheet popup on an actual phone (below ~768px width).

SECTION Q3 — Multi-cart discoverability
- Make the "new cart / new customer" button clearly visible and obvious in the saved-carts bar, so it is clear that each customer has a separate cart.
- Do NOT change the multi-cart logic — only its visibility/clarity.

SECTION Q4 — Default expense account
- READ the current expense entry screen.
- Keep the account field (account linkage is required for correct balances) BUT pre-fill it automatically with the main cash account, and make it secondary/collapsible so the user does not have to think about it.

SECTION Q5 — Fix dialog closing
- Audit EVERY dialog / modal / popup in the whole app.
- Each one must have all three: a visible X close button + close on backdrop click + close on the Esc key.
- If a focus-trap hook already exists, keep using it; do not remove existing behavior.

ACCEPTANCE CRITERIA:
- `npx tsc --noEmit` → 0 errors.
- A cash sale completes in one tap; advanced options remain optional.
- No user-facing "الدفع" remains — replaced by "تسجيل البيع".
- Cart is a side panel on tablet, bottom-sheet on phone only.
- The new-cart button is clearly visible.
- Expense account is auto-filled.
- Every dialog closes via X, backdrop, and Esc.
- No unrelated code/behavior changed.

After each section commit. After Q5: `git push origin main`.